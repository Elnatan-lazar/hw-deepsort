# Homework assignment: Deep Sort

Write a function that accepts as input a deep structure, made of nested lists, dicts, sets and tuples, and returns a string describing the same structure sorted in all levels in ascending order (values in lists, tuples and sets; keys in dicts).
